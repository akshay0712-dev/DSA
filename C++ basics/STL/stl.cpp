/*
#Containers{
    pairs                                           pop  -> log(n)
    vector                                          push -> log(n)
    List                                            top  -> O(1)
    DQ
    Queue
    Priority Queue {max_heap, min_heap}
    Set -> (sorted, unique )
    Multi Set -> (sorted, not unique)
    unordered set -> (not sortred, unique)
    Map -> (key,value),(uniqie keys, sorted)
    Multi Map -> (duplicate Keys, sorted)
    unorderes Map -> (unique key, not_sorted)
}

#Algorithms{
sort(begin, end)

}

*/

#include iosteram;
